const ytdl = require('ytdl-core');

module.exports = {
    name: 'queue',
    aliases: ['q'],
    description: 'view the current queue',
    args: false,
    usage: ``,
    group: 'music',
    execute(message, args) {
        //TODO: view queue
    },
};