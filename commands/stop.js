const ytdl = require('ytdl-core');

module.exports = {
    name: 'stop',
    aliases: ['s'],
    description: 'pauses the music being played',
    args: false,
    usage: ``,
    group: 'music',
    execute(message, args) {
        //TODO: stop the music
    },
};
