const {prefix} = require('../config.json');

module.exports = {
    name: 'dice',
    aliases: ['dd'],
    args: true,
    description: 'rolls virtual dice. great for d&d',
    usage: `dice_size number_of_rolls\``,
    execute(message, args) {
        // diiiiiiice
    },
};